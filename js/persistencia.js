const saveTasks = (tasks) => {
    //Aqui puedes utilizar esta función para guardar en LS o con fetch en mockAPI
}

const deleteTask = (id) => {
    //Aqui puedes utilizar esta función para eliminar en LS o con fetch en mockAPI
}

export {
    saveTasks,
    deleteTask
}